{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../contract && npm run build && cd ../frontend && tsc -b && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
